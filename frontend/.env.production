# 生产构建期的前端配置（两个值都是非 secret，会内联进浏览器包）。
#
# 注意：本地开发请用 .env.local 覆盖（它优先级更高，且不入库）。
# 这份文件入库是为了让「Git 推送 → 平台自动构建」出来的站点天然指向正确的后端，
# 不依赖在托管平台控制台里手填环境变量。
#
# NEXT_PUBLIC_API_BASE_URL 在 build 期被内联，改了必须重新构建才生效。
NEXT_PUBLIC_API_BASE_URL=https://travelplan-api.onrender.com
NEXT_PUBLIC_USE_MOCK_API=false
NEXT_PUBLIC_AMAP_KEY=
