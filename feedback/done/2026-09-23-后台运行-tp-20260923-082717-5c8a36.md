# 2026-09-23 管理后台运行复核

运行：`tp-20260923-082717-5c8a36`

## 已确认的问题

1. 该运行虽完成，但记录了 10 次 Provider 失败、14 条 Bad Case。机票与景区门票的途牛 CLI 成功信封被旧部署判为 `INVALID_RESPONSE`；当前 `main` 已包含兼容 `result.content[].text` 的解析，需随下一次 Render 自动部署生效。
2. Agent 可以把没有逐项来源的市内交通金额标成 `realtime`。本次“机场→酒店”和“市区→机场”均因此触发 `hallucinated_price`。

## 本次修复

- `submit_final_plan` 的单项增加 `source_ids`；提示词要求实时价格逐项引用交卷来源。
- 后端在落库前校验：实时价格没有可追溯来源时清空金额、改为 `unknown` 并给出风险说明，不把无依据数字写入行程或预算。
- 增加回归测试覆盖该降级路径。

## 未修改的历史数据

这次只修复后续运行的写入规则；既有 run 保留其原始审计事实，不回写或掩盖历史 Bad Case。